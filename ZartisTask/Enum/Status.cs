namespace ZartisTask.Enum
{
    /// <summary>
    /// this enum used for check the status of the slots
    /// </summary>
    public enum Status
    {
        Avilable = 1,
        NotAvilable = 2,
        Clash = 3
    }
}